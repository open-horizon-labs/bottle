[workspace]
members = ["cargo:."]

# Note: We use custom GitHub Actions workflows instead of cargo-dist generated CI.
# See .github/workflows/release.yml and .github/workflows/ci.yml
# This file is kept for reference but CI generation is disabled.

[dist]
# Target platforms (for reference)
targets = ["aarch64-apple-darwin", "x86_64-apple-darwin"]
